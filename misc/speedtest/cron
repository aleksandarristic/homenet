# Run speedtest every 15 minutes
*/15 * * * * DB_USER=<user> DB_PASSWORD=<password> DB_DATABASE=<database> /path/to/your/speedtest.py
# Run ping test every minute
*/1 * * * * DB_USER=<user> DB_PASSWORD=<password> DB_DATABASE=<database> /path/to/your/ping.py